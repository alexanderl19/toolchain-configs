{
  "name": "toolchain-configs",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format:check": "turbo run format:check && prettier --check .",
    "format:write": "turbo run format:write && prettier --write .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@alexanderliu/prettier-config": "workspace:*",
    "@changesets/cli": "2.27.1",
    "eslint": "9.2.0",
    "prettier": "3.2.5",
    "turbo": "1.13.3"
  },
  "packageManager": "pnpm@9.0.5",
  "engines": {
    "node": ">=18"
  }
}
